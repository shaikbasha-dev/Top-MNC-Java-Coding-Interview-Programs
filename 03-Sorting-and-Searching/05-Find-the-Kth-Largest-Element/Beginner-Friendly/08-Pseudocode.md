# 08 — Pseudocode

```text
START
    Create a min-heap.
    Add each array element.
    If heap size exceeds k, remove the smallest element.
    After all elements are processed, the heap root is the Kth largest.
    DISPLAY result
END
```

## Purpose

The pseudocode describes the algorithm without Java syntax. It should be understandable even before reading the implementation.
