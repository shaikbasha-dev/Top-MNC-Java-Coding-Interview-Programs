# 07 — Algorithm

1. Create a boolean table where dp[i][sum] means a target sum can be formed using the first i numbers.
2. Sum zero is always possible.
3. For each number, choose exclude or include when it fits.
4. Return the target state.
