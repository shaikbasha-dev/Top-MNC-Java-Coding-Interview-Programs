# Time and Space Complexity — Maximum Sum Contiguous Subarray

## Time Complexity

**Time Complexity: `O(n)`**

### Why?

The array is traversed once while maintaining the best ending sum and overall sum.

## Space Complexity

**Space Complexity: `O(1)`**

### Why?

The extra memory used by the algorithm is constant unless the algorithm explicitly creates an additional array.

## Interview Point

Always explain not only the Big-O value but also **why** the number of operations grows in that way.
