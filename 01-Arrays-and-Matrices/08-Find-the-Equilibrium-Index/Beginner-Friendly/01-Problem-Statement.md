# Problem Statement — Find the Equilibrium Index

## Complete Question

Find an index where the sum of elements on the left equals the sum of elements on the right.

You are given the following example input:

```text
{-7, 1, 5, 2, -4, 3, 0}
```

You must process the input according to the rules of the problem and produce:

```text
Equilibrium Index = 3
```

## What Is the Problem Asking?

The program must examine the supplied data, apply the required rule, and calculate the requested result.

## Input Description

The input represents the values that the algorithm must process. In the Java program, the example input is stored directly in an array or matrix so that the program can be executed without interactive input.

## Required Output

The program must display the requested result clearly.

## Important Rules

1. Process the values according to the problem definition.
2. Do not violate the order or relationship required by the problem.
3. Continue processing until all required values have been considered.
4. Produce the final result in a clear form.

## Example Explanation

The example is chosen so that the important part of the algorithm can be seen clearly. The program reads the values, applies its conditions and calculations, and finally prints the required result.

## Beginner-Friendly Interpretation

Think of the problem as a sequence of small decisions. We do not need to understand the optimized solution immediately. First, understand what information we have, what result is required, and what must happen to each value.
