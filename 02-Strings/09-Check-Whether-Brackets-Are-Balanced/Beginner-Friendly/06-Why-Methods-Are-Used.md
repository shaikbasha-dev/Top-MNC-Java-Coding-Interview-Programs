# Why Methods Are Used — Check Whether Brackets Are Balanced

## Why Methods Are Used in This Program

1. main()
2. System.out.print()
3. System.out.println()
4. charAt()
5. length()
6. push()
7. pop()
8. isEmpty()

## Detailed Method Explanations

### main()

#### General Definition
`main()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `main()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of main()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `main()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### System.out.print()

#### General Definition
`System.out.print()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `System.out.print()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of System.out.print()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `System.out.print()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### System.out.println()

#### General Definition
`System.out.println()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `System.out.println()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of System.out.println()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `System.out.println()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### charAt()

#### General Definition
`charAt()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `charAt()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of charAt()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `charAt()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### length()

#### General Definition
`length()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `length()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of length()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `length()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### push()

#### General Definition
`push()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `push()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of push()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `push()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### pop()

#### General Definition
`pop()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `pop()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of pop()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `pop()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.

### isEmpty()

#### General Definition
`isEmpty()` performs a specific Java operation used by the program.

#### Simple Explanation
In simple terms, `isEmpty()` helps the program perform the operation represented by its name.

#### General Example
```java
// Example usage of isEmpty()
```

#### Why It Is Used in This Program
This operation is used where the program needs the behavior provided by `isEmpty()`.

#### What Happens If We Do Not Use It
The program would need another valid way to perform the same operation; otherwise that part of the required logic would not be completed.
